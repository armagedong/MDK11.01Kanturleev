<Window x:Class="alga.DB.ImageListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:alga.DB"
        mc:Ignorable="d"
        Title="ImageListWindow" Height="450" Width="800">
    <Grid>
        <ListView x:Name="ImageList" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                  SelectionMode="Single" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ImageId}" Width="50"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Select" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Width="80" 
                Click="SelectImage"/>
    </Grid>
</Window>
